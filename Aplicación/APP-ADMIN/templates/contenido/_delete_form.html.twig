<form method="post" action="{{ path('contenido_delete', {'idContenido': contenido.id}) }}" onsubmit="return confirm('¿Segura de que quieres eliminar?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenido.id) }}">
    <button class="btn-blue">Eliminar</button>
</form>
