<form method="post" action="{{ path('evento_delete', {'idEvento': evento.id}) }}" onsubmit="return confirm('¿Segura de que quieres eliminar?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evento.id) }}">
    <button class="btn">Eliminar</button>
</form>
